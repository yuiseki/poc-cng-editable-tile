apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: poc-cng-editable-tile-edits
  namespace: knative-pool
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
